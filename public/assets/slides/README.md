# Hero slide assets

Replace `slide-1.svg` through `slide-5.svg` with your own optimized WebP, AVIF, JPG, or SVG images. Keep the filenames or update the `heroSlides` data and the hero component together.
