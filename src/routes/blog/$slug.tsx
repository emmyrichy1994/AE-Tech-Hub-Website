import { createFileRoute, Link, notFound } from '@tanstack/react-router'
import { posts } from '../../data/content'
export const Route=createFileRoute('/blog/$slug')({loader:({params})=>{const post=posts.find(p=>p.slug===params.slug);if(!post)throw notFound();return post},component:Article})
function Article(){const p=Route.useLoaderData();return <article><header className="bg-slate-950 pb-20 pt-40 text-white"><div className="shell max-w-4xl"><p className="eyebrow !text-teal-200">{p.category} · {p.time}</p><h1 className="mt-4 text-4xl font-semibold tracking-tight sm:text-6xl">{p.title}</h1><p className="mt-6 text-slate-300">By AE Tech Hub · {p.date}</p></div></header><div className="shell max-w-4xl py-16"><div className="h-72 rounded-3xl bg-gradient-to-br from-brand to-slate-900 sm:h-96"/><div className="prose mt-12 max-w-none text-lg leading-8 text-slate-700"><p>{p.excerpt}</p><h2 className="mt-10 text-3xl font-semibold text-slate-950">Start with the people you want to serve.</h2><p>Good digital experiences do more than look polished. They make the next step obvious, reduce friction and respect the time of the people using them. That means grounding every decision in a clear understanding of audience and intent.</p><p>At AE Tech Hub, we pair strong foundations with a willingness to question assumptions. The result is work that feels considered today and remains useful as your business changes.</p><h2 className="mt-10 text-3xl font-semibold text-slate-950">Make the important action easy.</h2><p>Clarity is a competitive advantage. A focused message, an honest value proposition and a clean route to action will always outperform noise.</p></div><Link to="/blog" className="mt-12 inline-block font-semibold text-brand">← Back to journal</Link></div></article>}
