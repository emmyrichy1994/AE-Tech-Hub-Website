import { createFileRoute, Link } from '@tanstack/react-router'
import { services } from '../data/content'
import { Reveal } from '../components/Reveal'
export const Route=createFileRoute('/services')({component:Services})
function Services(){return <><section className="bg-slate-950 pb-24 pt-40 text-white"><div className="shell"><p className="eyebrow !text-teal-200">Capabilities</p><h1 className="mt-4 max-w-3xl text-5xl font-semibold tracking-tight sm:text-6xl">The right expertise, exactly where it counts.</h1><p className="mt-6 max-w-xl text-slate-300">From a first impression to the systems that sustain it, we bring design and technology together.</p></div></section><section className="shell py-24"><div className="grid gap-5 md:grid-cols-2 lg:grid-cols-3">{services.map(s=><Reveal key={s.title}><article className="card group h-full p-7 transition hover:-translate-y-1 hover:shadow-lg"><span className="text-3xl text-brand">{s.icon}</span><h2 className="mt-6 text-xl font-semibold">{s.title}</h2><p className="mt-3 leading-7 text-slate-600">{s.description}</p></article></Reveal>)}</div></section><section className="bg-teal-50 py-20 text-center"><h2 className="text-3xl font-semibold">Not sure where to begin?</h2><p className="mt-3 text-slate-600">We can help you prioritise the work that will make the biggest difference.</p><Link to="/contact" className="btn btn-primary mt-6">Talk to us</Link></section></>}
